server:
  port: 8084

spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/FREEPDB1
    username: cursor1
    password: cursor1
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.Oracle12cDialect

jwt:
  secret: YOUR_JWT_SECRET
  expiration: 3600000 # 1 hour in ms for provider, 1800000 for patient 